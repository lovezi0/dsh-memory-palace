# 依赖
node_modules/

# 构建产物：lib/ 随仓库分发（git 渠道装即用，避免 pnpm 拦截 prepare 构建脚本）
# 注意：改动 src/ 后必须 npm run build 并连同 lib/ 一起提交

# 日志
*.log
npm-debug.log*

# 系统文件
.DS_Store
Thumbs.db

# 编辑器
.idea/
.vscode/

# WorkBuddy / Harness 工具数据目录（本机开发笔记，不进仓库）
.workbuddy/
.deepseek-harness/

# 本地测试数据（不入库）
testdata/

# 版本计划快照（按版本号归档，不进仓库，仅本地回归检索用）
plans/

# 升级计划
upgrade plan/